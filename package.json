{
  "name": "listenwrite",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "esbuild src/app.js --bundle --format=esm --target=safari16 --outfile=app.bundle.js"
  },
  "dependencies": {
    "ts-fsrs": "5.4.1"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  }
}
