name: Build Listenwrite v3

on:
  push:
    branches: [scheduler-v2-fsrs, main, v27-text-library]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/apply-v27.py
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: node --check src/app.js
      - run: node --check src/textlibrary.js
      - run: npm test
      - run: npm run build
      - name: Commit v27 source and browser bundle
        run: |
          if [ -z "$(git status --porcelain -- src/app.js src/textlibrary.js tests/browser-smoke.test.js tests/v27.test.js index.html app.bundle.js)" ]; then exit 0; fi
          git config user.name "listenwrite-bot"
          git config user.email "actions@users.noreply.github.com"
          git add src/app.js src/textlibrary.js tests/browser-smoke.test.js tests/v27.test.js index.html app.bundle.js
          git commit -m "Organize text library and restore simple words"
          git push
